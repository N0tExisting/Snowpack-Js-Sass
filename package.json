{
  "name": "snowpack-js-sass",
  "version": "0.0.1",
  "main": "plugin.js",
  "scripts": {
    "start": "tsc -p ./ -w",
    "build": "tsc -p ./",
    "test": "jest",
    "postinstall": "cd test/fixture && yarn --force && cd ../valid && yarn --force --emoji true"
  },
  "license": "MIT",
  "dependencies": {
    "sass": "^1.35.2",
    "snowpack": "^3.8.3"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "@types/node": "^15.14.8",
    "@types/sass": "^1.16.1",
    "jest": "^27.0.6",
    "typescript": "^4.3.5"
  }
}
